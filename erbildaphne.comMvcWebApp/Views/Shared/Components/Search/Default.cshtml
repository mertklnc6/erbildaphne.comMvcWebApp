@model SearchViewModel

<!-- ====== start popular posts ====== -->
<section class="tc-popular-posts-blog">
    @if (Model != null)
    {


        @if (Model.GNews.Count > 0 || Model.Articles.Count > 0 || Model.Rumors.Count > 0)
        {
            <div class="container">
                <div class="content-widgets pt-50 pb-50">
                    <div class="row">
                        <div class="col-lg-9 pe-lg-5">
                            <div class="tc-post-list-style3">
                                <div class="items">
                                    @foreach (var item in Model.Articles)
                                    {
                                        @if (item.IsPublished && !item.IsDeleted)
                                        {

                                            <div class="item">
                                                <div class="row">
                                                    <div class="col-lg-5">
                                                        <div class="img th-230 img-cover overflow-hidden">
                                                            <img src="@item.PictureUrl" alt="@item.Title">
                                                            <a asp-action="Details" asp-controller="Article" asp-route-id="@item.Id" class=" icon-60 border-2">
                                                            </a>
                                                        </div>
                                                    </div>
                                                    <div class="col-lg-7">
                                                        <div class="content mt-20 mt-lg-0">

                                                            <h4 class="title fw-bold">
                                                                <a asp-action="Details" asp-controller="Article" asp-route-id="@item.Id" class="hover-underline">
                                                                    @item.Title
                                                                </a>
                                                            </h4>
                                                            <div class="text color-666 mt-20">
                                                                @item.Description
                                                            </div>
                                                            <div class="meta-bot fsz-13px color-666">
                                                                <ul class="d-flex">
                                                                    <li class="date me-5">
                                                                        <i class="la la-calendar me-2"></i> @item.CreatedDate.ToShortDateString()
                                                                    </li>
                                                                </ul>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-lg-3">
                            <div class="widgets widgets-sticky mt-5 mt-lg-0">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="container">
                <div class="content-widgets pt-50 pb-50">
                    <div class="row">
                        <div class="col-lg-9 pe-lg-5">
                            <div class="tc-post-list-style3">
                                <div class="items">
                                    @foreach (var item in Model.GNews)
                                    {
                                        @if (item.IsPublished && !item.IsDeleted)
                                        {

                                            <div class="item">
                                                <div class="row">
                                                    <div class="col-lg-5">
                                                        <div class="img th-230 img-cover overflow-hidden">
                                                            <img src="@item.PictureUrl" alt="@item.Title">
                                                            <a asp-action="Details" asp-controller="Article" asp-route-id="@item.Id" class=" icon-60 border-2">
                                                            </a>
                                                        </div>
                                                    </div>
                                                    <div class="col-lg-7">
                                                        <div class="content mt-20 mt-lg-0">

                                                            <h4 class="title fw-bold">
                                                                <a asp-action="Details" asp-controller="Article" asp-route-id="@item.Id" class="hover-underline">
                                                                    @item.Title
                                                                </a>
                                                            </h4>
                                                            <div class="text color-666 mt-20">
                                                                @item.Description
                                                            </div>
                                                            <div class="meta-bot fsz-13px color-666">
                                                                <ul class="d-flex">
                                                                    <li class="date me-5">
                                                                        <i class="la la-calendar me-2"></i> @item.CreatedDate.ToShortDateString()
                                                                    </li>
                                                                </ul>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-lg-3">
                            <div class="widgets widgets-sticky mt-5 mt-lg-0">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="container">
                <div class="content-widgets pt-50 pb-50">
                    <div class="row">
                        <div class="col-lg-9 pe-lg-5">
                            <div class="tc-post-list-style3">
                                <div class="items">
                                    @foreach (var item in Model.Rumors)
                                    {
                                        @if (item.IsPublished && !item.IsDeleted)
                                        {

                                            <div class="item">
                                                <div class="row">
                                                    <div class="col-lg-5">
                                                        <div class="img th-230 img-cover overflow-hidden">
                                                            <img src="@item.PictureUrl" alt="@item.Title">
                                                            <a asp-action="Details" asp-controller="Article" asp-route-id="@item.Id" class=" icon-60 border-2">
                                                            </a>
                                                        </div>
                                                    </div>
                                                    <div class="col-lg-7">
                                                        <div class="content mt-20 mt-lg-0">

                                                            <h4 class="title fw-bold">
                                                                <a asp-action="Details" asp-controller="Article" asp-route-id="@item.Id" class="hover-underline">
                                                                    @item.Title
                                                                </a>
                                                            </h4>
                                                            <div class="text color-666 mt-20">
                                                                @item.Description
                                                            </div>
                                                            <div class="meta-bot fsz-13px color-666">
                                                                <ul class="d-flex">
                                                                    <li class="date me-5">
                                                                        <i class="la la-calendar me-2"></i> @item.CreatedDate.ToShortDateString()
                                                                    </li>
                                                                </ul>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-lg-3">
                            <div class="widgets widgets-sticky mt-5 mt-lg-0">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
        else
        {
            <div class="card border-danger mx-auto mt-5" style="max-width: 40rem;">
                <div class="card-header bg-danger text-white">Arama Sonucu</div>
                <div class="card-body text-danger">
                    <h5 class="card-title">Sonuç Bulunamadı</h5>
                    <p class="card-text">Aramanıza ait herhangi bir sonuç bulunamadı. Lütfen farklı anahtar kelimelerle tekrar deneyin.</p>
                </div>
            </div>
        }
    }
    else
    {
        <div class="card border-danger mx-auto mt-5" style="max-width: 40rem;">
            <div class="card-header bg-danger text-white">Arama Sonucu</div>
            <div class="card-body text-danger">
                <h5 class="card-title">Sonuç Bulunamadı</h5>
                <p class="card-text">Aramanıza ait herhangi bir sonuç bulunamadı. Lütfen farklı anahtar kelimelerle tekrar deneyin.</p>
            </div>
        </div>
        <br />
    }

</section>
<br />
<!-- ====== end popular posts ====== -->
